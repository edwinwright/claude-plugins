---
ticket_type: story | technical
phase: 1
depends_on: []
epic: _epic.md
---

# [Story Title]

**Type:** Story | Technical Story
**Epic:** [Epic title]
**Phase:** [Phase number]
**Dependencies:** [Filenames of stories that must be complete before this one, or "None"]

---

## Description

What this story delivers and why, from the user's perspective. For Technical stories, describe what is being built and why it is needed before feature work can begin.

Write this so it stands on its own. The requirements document and tech spec are transient — they are archived once the work is accepted — so a reader coming to this ticket later has only what is written here.

## Technical Context

Background for the implementer — relevant architecture decisions, existing patterns to follow, constraints carried over from the design. Keep it to what is non-obvious or easily missed.

Enough *why* to support a judgement call. An implementer who hits something the tasks did not anticipate should be able to work out what the story would have wanted. "Use the existing `useOptimistic` pattern" is a rule; "the list must not visibly reorder while a mutation is in flight, which is why we go optimistic here" lets someone solve a problem the rule did not cover.

Link only to **durable** documents — `docs/product/glossary.md`, `docs/product/domain-model.md`, `docs/product/nfr.md`, `docs/decisions/`. Do not link to the requirements document or tech spec; those links break when the work order is archived.

## Files

Where this story's work goes. Real paths, not component names.

| Path | Change |
|---|---|
| | Create / Modify |

**Do not touch:** [Paths an implementer might reasonably think are in scope but are not, and why. Or "Nothing flagged."]

---

## Tasks

Include only the sections that apply to this story. Remove any that are not relevant.

### Frontend

- [ ] [Specific task — e.g. "Add LikeButton component to CommentCard"]
- [ ] [Specific task]

### Backend

- [ ] [Specific task — e.g. "Add POST /api/comments/:id/like endpoint"]
- [ ] [Specific task]

### Database

- [ ] [Specific task — e.g. "Create likes table migration"]
- [ ] [Specific task]

### Other

- [ ] [Any task that does not fit the above — e.g. infrastructure, third-party config]

---

## Acceptance Criteria

Every criterion names the check that proves it. These are the contract `acceptance-review` verifies against when the work order is closed out, so a criterion nobody can check is a criterion nobody will honour.

- [ ] **Given** [context], **when** [action], **then** [outcome]
      **Verify:** `[command or test name]`
- [ ] **Given** [context], **when** [action], **then** [outcome]
      **Verify:** `[command or test name]`

Cover the important error and edge cases, not only the happy path.

Where something genuinely cannot be checked by a command, write `Verify: manual — [exactly what a person does and what they should see]`. That is a legitimate answer; leaving the line off is not.

---

## Verification

Run these to prove the story is done. Copy the exact commands from the tech spec's Verification Commands block, or from `AGENTS.md` where there is no tech spec.

```
lint:  [command]
test:  [command]
build: [command]
```

Anything requiring a person rather than a command:

- [ ] [Manual check, and what a correct result looks like]

---

## Definition of Done

- [ ] All tasks above are complete
- [ ] Every acceptance criterion passes its `Verify` check
- [ ] The commands in Verification all pass
- [ ] Code has been reviewed
- [ ] Feature is deployed to a staging or preview environment

---

*Generated by the `work-breakdown` skill. Publish to Linear or GitHub with `ticket-publish`; close out with `acceptance-review` once the work is built.*
